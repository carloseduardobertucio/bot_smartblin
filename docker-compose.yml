version: '3.8'

services:
  chatbot:
    build: .
    container_name: smartblin-chatbot
    restart: unless-stopped
    tty: true
    stdin_open: true
    privileged: true
    security_opt:
      - seccomp=unconfined
    volumes:
      - .:/app
      - /app/node_modules 
